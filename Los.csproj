<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Library</OutputType>
    <AssemblyName Condition="'$(ClientRegion)' == 'CN'">Los</AssemblyName>
    <AssemblyName Condition="'$(ClientRegion)' == 'TC'">Los.TC</AssemblyName>
    <RootNamespace>LosPr</RootNamespace>
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <Deterministic>true</Deterministic>
    <CopyLocalLockFileAssemblies>false</CopyLocalLockFileAssemblies>
  </PropertyGroup>

  <ItemGroup>
    <Compile Remove="Tests\**\*.cs" />
    <EmbeddedResource Include="BLM\UI\Assets\BlackCatFamiliar.png"
                      LogicalName="LosPr.BLM.UI.Assets.BlackCatFamiliar.png" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="PromeRotation.SDK.API15"
                      Version="0.1.0-preview.8"
                      Condition="'$(ClientRegion)' == 'CN'"
                      PrivateAssets="all" />
    <PackageReference Include="PromeRotation.SDK.TC"
                      Version="0.1.0-preview.2"
                      Condition="'$(ClientRegion)' == 'TC'"
                      PrivateAssets="all" />
  </ItemGroup>
</Project>
